# Changelog

## [0.2.0] - 2026-07-07
### Changed
- Search with a relative stop directory now stops at that directory and no longer returns config files from its parent directories (#20)
- Load results for absolute paths that start with `/..` now keep the leading root in `filepath` (#21)

## [0.2.0] - 2026-07-07
### Changed
- Search with a relative stop directory now stops at that directory and no longer returns config files from its parent directories (#20)
- Load results for absolute paths that start with `/..` now keep the leading root in `filepath` (#21)
